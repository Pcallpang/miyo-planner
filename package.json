{
  "name": "haru-planner",
  "private": true,
  "version": "1.0.0",
  "description": "교사용 개인 일정·To-Do 관리 웹앱 (구글 캘린더 + Gemini 연동)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test": "node --test server/lib/*.test.js && npm run test -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
